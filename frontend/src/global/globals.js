let classSelector = null;

export const setClassSelector = (value) => {
    classSelector = value;
};

export const getClassSelector = () => {
    return classSelector;
};

//
let quizCount = null;

export const setQuizCount = (value) => {
    quizCount = value;
};

export const getQuizCount = () => {
    return quizCount;
};
